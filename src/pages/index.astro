---
import BaseLayout from "../layouts/BaseLayout.astro";
import { features, installOptions, themeShots } from "../data/site.js";

const heroTitle = "A three-pane terminal file manager with rich previews and inline images";
const quickStartOptions = installOptions.map(({ name, command }) => ({ name, command }));
---

<BaseLayout
  title="Elio | Terminal File Manager"
  description="A terminal-native file manager with a three-pane layout, rich previews, and inline images."
>
  <section class="hero">
    <div class="container hero">
      <div class="hero-header">
        <p class="eyebrow">ELIO FM</p>
        <h1>{heroTitle}</h1>
      </div>

      <div class="hero-grid">
        <div class="hero-copy">
        <p class="hero-text">
          Built for fast workflows with bulk actions and customizable places.
        </p>

        <div class="hero-actions">
          <a class="button button-primary" href="/install">Install Elio</a>
          <a class="button button-secondary" href="https://github.com/elio-fm/elio" target="_blank" rel="noreferrer">
            GitHub
          </a>
        </div>
        </div>

        <div class="hero-shot frame">
          <div class="frame-bar">
            <span></span>
            <span></span>
            <span></span>
          </div>
          <div class="frame-body">
            <img
              src="/images/default.webp"
              alt="Default Elio interface showing places, files, and preview panes."
              width="1425"
              loading="eager"
            />
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="section-heading">
        <p class="eyebrow">VISUALS</p>
        <h2>The interface tells the story.</h2>
        <p>
          Bundled themes show how much the interface can shift while staying readable.
        </p>
      </div>

      <div class="visual-grid">
        {
          themeShots.map((shot) => (
            <figure class="shot-card">
              <img src={shot.image} alt={shot.alt} loading="lazy" />
              <figcaption>{shot.name}</figcaption>
            </figure>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="section-heading">
        <p class="eyebrow">FEATURES</p>
        <h2>Built for fast navigation.</h2>
      </div>

      <div class="feature-grid">
        {
          features.map((feature) => (
            <article class="feature-card">
              <h3>{feature.title}</h3>
              <p>{feature.body}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="section-heading">
        <p class="eyebrow">QUICK START</p>
        <h2>Install in one command.</h2>
      </div>

      <div class="quick-start" data-quick-start>
        <div class="quick-start-tabs" role="tablist" aria-label="Install methods">
          {
            quickStartOptions.map((option, index) => (
              <button
                class:list={["quick-start-tab", index === 0 && "is-active"]}
                type="button"
                role="tab"
                aria-selected={index === 0 ? "true" : "false"}
                data-command={option.command}
              >
                {option.name}
              </button>
            ))
          }
        </div>

        <div class="quick-start-panel">
          <pre><code data-quick-start-command>{quickStartOptions[0].command}</code></pre>
          <a class="text-link" href="/install">Open full install page</a>
        </div>
      </div>
    </div>
  </section>

  <script>
    const root = document.querySelector("[data-quick-start]");
    if (root) {
      const tabs = Array.from(root.querySelectorAll(".quick-start-tab"));
      const command = root.querySelector("[data-quick-start-command]");

      const selectTab = (activeTab) => {
        tabs.forEach((tab) => {
          const isActive = tab === activeTab;
          tab.classList.toggle("is-active", isActive);
          tab.setAttribute("aria-selected", isActive ? "true" : "false");
        });

        if (command) {
          command.textContent = activeTab.dataset.command ?? "";
        }
      };

      tabs.forEach((tab) => {
        tab.addEventListener("click", () => selectTab(tab));
      });
    }
  </script>
</BaseLayout>
