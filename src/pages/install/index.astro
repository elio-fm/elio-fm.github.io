---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { installOptions } from "../../data/site.js";
import { siArchlinux, siFedora, siHomebrew, siRust } from "simple-icons";
import { Check, Copy } from "lucide-static";

const formatInstallIcon = (svg) =>
  svg
    .replace(/<title>.*?<\/title>/, "")
    .replace("<svg ", '<svg focusable="false" aria-hidden="true" preserveAspectRatio="xMidYMid meet" ');

const installIcons = {
  archlinux: formatInstallIcon(siArchlinux.svg),
  fedora: formatInstallIcon(siFedora.svg),
  homebrew: formatInstallIcon(siHomebrew.svg),
  rust: formatInstallIcon(siRust.svg),
};
---

<BaseLayout
  title="Install elio"
  description="Install elio with Homebrew, AUR, Fedora COPR, or Cargo."
>
  <section class="page-intro page-intro-compact">
    <div class="container">
      <div class="page-lead">
      <p class="eyebrow">INSTALL</p>
      <h1>Install elio</h1>
      <p>
        Use your preferred package manager.
      </p>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="install-page-grid">
        <div class="install-list">
          {
            installOptions.map((option) => (
              <article class="install-card">
                <div class="install-topline">
                  <h2 class="install-title">
                    <span class={`install-icon install-icon--${option.icon}`} aria-hidden="true">
                      <span class="install-icon-mark" set:html={installIcons[option.icon]}></span>
                    </span>
                    <span>{option.name}</span>
                  </h2>
                  <p>{option.note}</p>
                </div>
                <div class="command-block" data-command-block>
                  <button
                    class="command-copy"
                    type="button"
                    data-copy-command
                    data-copy-state="idle"
                    aria-label="Copy command"
                    title="Copy command"
                  >
                    <span class="command-copy-icon" data-copy-icon="copy" aria-hidden="true" set:html={Copy}></span>
                    <span class="command-copy-icon" data-copy-icon="check" aria-hidden="true" set:html={Check}></span>
                  </button>
                  <pre><code>{option.command}</code></pre>
                </div>
              </article>
            ))
          }
        </div>

        <aside class="install-aside">
          <article class="doc-card">
            <h2>Starting directory</h2>
            <p>elio opens in your current working directory.</p>
          </article>

          <article class="doc-card">
            <h2>Nerd Font</h2>
            <p>Use a Nerd Font in your terminal so file icons render correctly.</p>
          </article>

          <article class="doc-card">
            <h2>Run from source</h2>
            <p>Clone the repo, then run elio from the project directory.</p>
            <div class="command-block" data-command-block>
              <button
                class="command-copy"
                type="button"
                data-copy-command
                data-copy-state="idle"
                aria-label="Copy command"
                title="Copy command"
              >
                <span class="command-copy-icon" data-copy-icon="copy" aria-hidden="true" set:html={Copy}></span>
                <span class="command-copy-icon" data-copy-icon="check" aria-hidden="true" set:html={Check}></span>
              </button>
              <pre><code>{`git clone https://github.com/elio-fm/elio.git
cd elio
cargo run --release`}</code></pre>
            </div>
          </article>

          <article class="doc-card">
            <h2>Docs and README</h2>
            <p>
              For configuration, theming, and the full project reference, use the docs and README.
            </p>
            <p><a href="/docs">Open docs</a></p>
            <p>
              <a href="https://github.com/elio-fm/elio#readme" target="_blank" rel="noreferrer">
                Read the README
              </a>
            </p>
          </article>
        </aside>
      </div>
    </div>
  </section>
</BaseLayout>
